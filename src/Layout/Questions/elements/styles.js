import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles(theme => ({
    root:{
        display:'flex',
        flexDirection:'row',
        width:'50%',
    },
    textfield:{
        
    }
}));

export default useStyles;